{{! template-lint-disable no-implicit-this }}
{{#in-element destinationEl insertBefore=null}}
  <PaperToastInner @swipeToClose={{this.swipeToClose}} @onClose={{this.onClose}} @top={{this.top}} @left={{this.left}} @capsule={{this.capsule}} @class={{this.class}}>
    {{yield (hash
      text=(component "paper-toast-text")
    )}}
  </PaperToastInner>
{{/in-element}}
